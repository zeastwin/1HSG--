#### 软件更新记录

#### 2022.11.1—2022.11.30（E5032总线卡）
1. 总线卡走点、插补、轨迹测试，增加IO立即输出。
2. 修改Z轴掉轴问题，应使能后延时再关刹车。
3. 修改圆弧插补、比较指令设置，使其更明了。
4. 总线卡一维高速启动就亮IO，二维高速IO无效--------驱动器不支持外接到ACC36—IO，所有的脉冲驱动器都支持，只有部分总线驱动器支持。
5. 解决暂停在走偏移量、料盘点时，工站状态是回原中/暂停中，取消暂停无动作，流程状态是工作中。
6. 多段插补会停顿，圆弧插补段加了直线插补点（已去掉），设置前瞻函数报错6012。
7. 修改插补运动暂停，工站状态仍是运动中，有轨迹中断现象，无报错，流程运行中，流程无法停止/重新运行，只能重启 。
8. 总线卡验证在线变位
9. 修改文件读取指令设置变量路径无效。

#### 2022.12.1—2022.12.15（Dmc脉冲卡）
1. 插补暂停有问题，工站就绪，流程报警-------------暂停了，但轴还在动，暂停后加延时。
2. 插补设置末速度，轨迹之间仍看到停顿(有前瞻，直接设置末速度)。
3. 低速比较的模式IO设置模式------ok，默认6.7.8模式，根据设置time识别，详细看雷赛文档或者指令说明。
4. 测试高速比较，不报错，单IO暂时无反应-----------线没接到编码器。
5. 插补暂停时，一直不断触发暂停，就会导致界面卡死，卡要重新冷复位。

#### 2022.12.15—2022.12.31（E3032总线卡）
1. 修改工站调试界面步进设置无效
2. 修改汇川机器人连续运动时一直处于运动中
3. 修改汇川机器人连续运动时，不更新当前位置
4. 修改点位之后，切换工站会弹框（6-7次）。
5. 修改总线卡3032卡在断电后再重启后，需要冷复位才连接上。
6. 修改指令点位修改后，在UI上不更新。
7. 修改工站z轴到正限后，不能复位。
8. 修改机器人在app示教后，没有更新到当前点文件。
9. log文件夹没有打印文档--------少了log4net.config。
10. 修改账户管理初次登录其他显示跟列表不对应。
11. 修改工站的轴信息不方便查看。
12. 模拟量指令读取不到-----------上一条运动指令需设置为不等待
13. 去掉log带_Log标志的需要打印详细信息。
14. 网络对象初始化后再反初始化后，重连时间耗时大概2分钟---实测30s左右
15. 修改变量模块一下子删除IO，崩溃，系统变量不见了。

#### 2023.1.10—2023.1.16
1. 修改主界面功能有错误, (UPH/CT)不能运行问题。
2. 修改工站名字可以重复问题。
3. 修改获取变量指令不能使用问题。
4. 串口增加了rts设置。
5. 汇川机器人增加jog运动，解决连续运动时u轴走到一定点位，会反转问题。
6. 网络通讯对象收发数据可保存到D://sktfile下的文件中。
7. 工站配置机器人名称不匹配，汇川用的是ABB，ABB纠正为INOVANCE。
8. 修改结构体设置-1，删除所有子项。
9. 修改网口跟串口切换，保存文件勾选项消失问题。
10. 汇川jog运动速度问题，机器人速度就是全局速度，0-100，不用计算百分比。
11. 修改信号检测运动一直报警。
12. 修改流程点位超过100，跳转出错问题。
13. 更改通讯对象状态显示图标。
14. 修改显示问题，弹框指令 ，IO检测指令, IO操作指令, 有时候会显示错误, 只有一个,显示4个。
15. 硬件配置页面去掉机器人不必要的配置。
16. 修改变量清空几个，其中有空行的则会清空不掉问题。
17. 变量模块列表表头可调整。

#### 2023.1.16—2023.1.30
1. 变量模块去掉整行的复制粘贴快捷键，剪切板设为disble。
2. 自动的情况下，增加提示，否则不知道为何保存不了。
3. 修改结构体删除第一项删除不掉，提示是最后一项的问题。
4. 解决通讯对象切换到串口页面，状态图标加载显示异常。
5. 发送及接收指令，清除缓存默认为true。
6. io列表禁用排序功能。
7. 解决串口相关接口在demo使用中会崩溃问题。

#### 2023.2.1—2023.2.7
1. PLC界面通讯ID根据通讯模块的配置，由输入型改成选择型。
2. 自动的情况下，增加弹框提示，否则不知道为何保存不了。
3. 修改流程页面，异常信息清除无效的问题。
4. 修改位置比较指令显示点位一直都是第一个工站的问题。
5. 完善点位修改指令显示问题。
6. Log作为基础属性来实现，加一个Z项，以后不太用的属性都到Z项去，去掉_Log的设置。
7. 解决修改变量二级地址, 新增修改变量不会隐藏的问题。

#### 2023.2.8—2023.2.12
1. 优化skt模块，解决接收数据丢失问题。
2. 串口优化，增加流控制RTS、DTS设置项，485转232会有收到数据部分错乱，需设置RTS为true。
3. 测试机用5032卡发现速度设置太快，速度超过4M频率，会报错-49。
4. 修改PLC调试串口通讯ID选择框崩溃的问题。
5. 修改没有登录的时候，可获取到当前用户的权限的问题。

#### 2023.2.13—2023.2.19
1. 修改流程操作指令，启动的流程已经启动，但指令执行显示错误问题。
2. 插补的末速度开放出来，在流程指令可设置。
3. 插补小段前瞻设置开放出来，在硬件配置页面可设置。
4. 硬件模块轴设置开放绝对编码器设置项，在硬件配置页面可设置。

#### 2023.2.20—2023.2.28
1. 插补增加速度曲线-dmc_set_vector_s_profile设置。
2. 插补增加减速度值，不再直接同于加速度。
3. 修改在连续编辑或删减IO配置和轴配置后, IO配置在重启软件会消失问题。
4. 统一工站速度加速的单位, 整理没有用或重复的工站运动参数。
5. 解决增加五轴功能后，卡初始化失败，导致IO无效问题。
6. 修改工站状态跳转指令，其之前跳转无效。
7. 汇川机器人优化示教点OK，但是流程走点应该走90，却反方向走270的问题。
8. 修改汇川机器人点位保存点位。
9. 模拟量增加鑫精城网口类型及测试。
10. 点位需求扩充，可根据需求插入点位。
11. 汇川机器人模块补充错误log。
12. 总线卡E3032，松下驱动器，到限位后运动停止，但雷赛状态一直在运动中，驱动器参数默认是出厂设置，需修改其参数为10000。
13. 修复汇川两个机器人有一个机器人在软件打开后状态为出错，重置后OK的问题。

#### 2023.3.1—2023.3.5
1. 汇川机器人增加手势显示。
2. 汇川机器人可手动加载控制器点位，手动加载时才加载。
3. 汇川机器人修改点位时，手势状态刷新不对。
4. 汇川机器人在软件初始化时加载控制器点位，两边点位统一。
5. 原先板卡基础上合并五轴脉冲卡。
6. 解决合并五轴后总线卡异常。

#### 2023.3.6—2023.3.13
1. 优化结构体模块结构项显示不清晰问题，结构体数据项去掉数据类型显示。
2. action有个别指令没有log打印，增加了指令的infolog。
3. 修复串口通讯接收不到消息。
4. 前瞻相关参数全部开放，可设置。
5. 增加总线模拟量模块。
6. 增加总线卡支持多插补系功能。
7. 点胶工站走完IO轨迹后，走点失败，伺服超载报警，因为IO跟卡不在同一张卡上。
8. 解决鑫精城网口模拟量读取不同通道数据是一样的问题。
9. 增加及测试总线卡读取绝对值编码器。
10. IO模块,删除所有卡时不会清空IO信息,导致保存了以前的信息跟现在的信息冲突。
11. 伺服获取的是编码器位置，其他为指令位置。
12. 完善流程页面轨迹显示。
13. 修复出现工站第1个轴点击使能按钮的时候，工站状态为伺服断电，整个页面禁用，无法操作的异常问题。

#### 2023.3.14—2023.3.19
1. 修复流程指令回原时，选择优先轴回原，不等待设置为true无效的问题。
2. 增加MTL位移传感器。
3. 板卡模块warnLog改为为errLog，指令log选择报警忽略跟自动处理时不打印log。

#### 2023.3.20
1. 完善总线卡模拟量模块读写功能。
2. 修复IO配置的一些问题。
3. 整理扩展IO模块。
4. 平台现有log信息完善，改成模块+调用函数+详细说明。
5. 模块错误码整理。
6. 总线卡E5032直线有插补不论速度设置多少，在启动插补的时候电机有异响ok，更新2022.6.30版本雷赛固件。
7. 修改总线卡E5032圆弧轨迹点报错问题。
8. 完善5800暂停出错问题。

#### 2023.3.23
1. 修改工站状态混乱，没检测到异常轴问题
2. 运动调试按钮容易混淆，进行优化。
3. 流程点位复制，点位值是空，修复OK。
4. 回原前偏移功能无效，已增加。
5. 轨迹添加直线点时，改造为插入连续点位，简化大规模点位导入时的运行效率及编辑效率
6. 工站点位需增加插入、删除功能。
7. 修改工站回原点指令设置不等待，点击流程停止按钮时，停止不了工站和流程的异常问题。
8. 修改工站某轴触发限位停止时其它轴没停止的问题。

#### 2023.3.27
1. 修复工站重新配置后，指令中点位重新选择后，点位索引不更新的问题。
2. 龙门双驱绝对编码器位置有时会变，是因为驱动器脉冲数默认设置过大，会造成溢出。
3. 增加固高总线卡模块

#### 2023.4.5
1. IO指令操作时间增加变量设置。
2. 分析判断流程状态--启动流程，流程启动会偶尔报错，是因为两个流程同时进行操作了同一个流程，导致报警显示被操作的流程状态是运行中，被操作的流程可设置变量来标识完成。
3. 流程页面系统log，修改界面布局。
4. 流程禁止打印信息、禁止、报警log无效。
   ​

#### 2023.4.10
1. 修改dmc_change_speed_unit() 经常报错3024问题。
2. 工站调试界面的走点需要设置为所选择的3档速度。
3. 修复指令属性显示问题，在指令编辑里面点击 "地址常量切换" 从变量切换成常量有概率不会清空变量。
4. 新版程序添加轨迹点时,有概率添加错误,并且走点的速度非常快, 设置速度没起作用--指令速度设置显示，会导致误判，显示不改变速度，实际是改变速度的
5. 工站里面速度选择的条不应该直接用ValueChanged事件, 应该加判断, 鼠标抬起时才改变。
6. 总线卡有轴报警，其他工站插补走不了，并且工站页面也没有报警提示，一直在刷stop。
7. E5032卡，单独走轨迹点OK，走连续几个点在下个点位开始前会出现突然加速或减速现象。
8. 前瞻设置更改位置，硬件设置 ->工站->前瞻参数设置。
9. 硬件设置去掉【单位每毫米】设置，现在当量设置需在雷赛卡配置那边设置。
10. 将版本记录以及一些注意事项/错误码表显示在平台某个地方，方便查看。

#### 2023.4.17
1. 增加dmc3000模块，兼容雷赛3000系列脉冲卡。
2. 修改3400卡进行更新后，点位跟之前相差较大问题。
3. 修改添加新的流程点位,指令类型下拉框有时需要多选几次才生效的问题。
4. 修改5000系列脉冲卡更新为无单位毫米脉冲设置的版本后，有轴回原失败的问题。
5. 工站调试页面放开【另存为】跟【加载】，以便保存跟加载点位需求。

#### 2023.4.24
 1. 完善PLC映射功能。
 2. 解决帮助页面表格显示问题。

#### 2023.5.8
1. 工站调试UI布局调整。
2. Dmc3000修改找限位距离，否则距离太短，走不到位。
3. 解决流程UI因流程节点字体设置导致的崩溃问题。
4. 服务器端的端口需要加防呆, 设置小于200的需要提示, 并且不能设置为0。
5. 修改指令属性项标题，可显示常量变量切换，且点击常量变量切换后会导致崩溃问题。

#### 2023.5.15 - 2023.5.31
1. 修复结构体结构项修改值或名称时弹框对不上的问题。
2. 轨迹指令中的直线插补类型增加点位倒序设置属性。
3. 在工站调试界面，增加按Ctrl+空格键停止工站的功能。
4. 优化模拟量读取接口，分为读取单个和多个模拟量接口。
5. 修改流程剥离之后，点击其他页面，显示崩溃问题。

####2023.6.1 - 6.12
1. 流程报警上报到业务层，由报警处理完后生效改成报警立即生效。
2. 修改在工站调试界面调整滚动条，会改变连续运动的方向的问题。
3. 合并固高总线卡。

#### 2023.6.19 - 6.23
1. 3000系列卡，走偏移量会报走不到位，实际已经到位。

#### 2023.6.25 - 6.30
1. 修复汇川机器人执行动作未就绪就接着下一条指令问题。
2. 汇川机器人偏移量增加插补模式。
3. 汇川机器人在切换以太网与示教器模式时，这时断电不报错。

#### 2023.7.1 - 7.31
1. 修改共用轴导致的暂停恢复不了的问题。
2. 增加轨迹到位判断。
3. 修复汇川4轴机器人连续运动失败的问题。
4. 汇川机器人go点速度在运动调试界面无法更改问题。
5. 解决模拟量读取温控器大概率会读取到-999999。

#### 2023.8.1 - 8.11
1. 汇川机器人go模式增加禁用轴功能。
2. 修改汇川机器人计算速度，与轴速度计算方式保持一致。
3. 修改等待运动就绪指令，检测机器人是否停止到某个点位，检测不了的问题。
4. 读取IO指令超时改为默认3000。
5. 获取日期时间指令，去掉固定格式，改成自定义，不做限制。
6. 回原指令增加单轴回原模式。
7. 工站增加暂停信号配置，需与暂停信号配对使用。
8. 修改PLC写字符串，把字符串的结尾全部用\0替换。

#### 2023.8.18 - 8.31
1. 结构体右键菜单插入数据项禁用。
2. 通讯发送及接受指令加锁。
3. 修改指令插入并且保存后，再选择当前指令进行修改，但是修改内容是当前指令的上一条的问题。
4. 修改连接指令在连接对象为初始化时执行无报警的问题。
5. 逻辑判断增加判断结构体值进行跳转功能。
6. PLC指令属性名称进行优化，首地址可以进行常量变量切换。

#### 2023.9.1 - 9.26
1. 汇川机器人增加立即暂停功能。
2. 变量进行分页，区分普通变量跟系统变量。
3. 报警页面配置信息，新旧版本不兼容，增加自动转化功能。
4. 修改回原指令选择优先轴回原时，有轴重复回原的问题。
5. 完善禁用工站暂停指令。
6. 解决脉冲卡在工站调试页面，走连续运动时，松回鼠标会报错问题。
7. 汇川机器人在调试界面的短中长距，把之前的走点模式修改为寸动模式。
8. 增加插补跟随指令。
9. 工站调试界面增加限位显示。

#### 2023.10.5 - 10.31
1. 用户登录失败，检查当前账户依然存在。
2. 解决变量翻页，查找最后一项崩溃等问题。
3. 变量增加监控列表保存，并能在点击时自动定位功能。
4. 增加工站限制功能。
5. 增加卓航压力传感器。
6. 解决总线卡位置比较添加多个比较点后，只触发一次的异常问题。
7. 变量滚动条记录及要实现点击刷新。
8. 报警弹框位置更改为弹在屏幕中央。

#### 2023.11.1 - 11.30
1. 增加模拟量写入功能。
2. 修改流程查找功能异常问题。
3. 走点、轨迹指令增加参考点设置。
4. IO配置表分输入输出。
5. 解决辅助编码器清不掉的问题。
6. 修改走点检查到位时间，延长至5分钟。
7. 点位增加多行删除功能。
8. 修改点位速度设置异常（显示常量但使用的是变量）问题。
9. 插补IO反转。
10. 点位单元格支持区域复制、粘贴功能。
11. 解决步骤信息对象不一致的问题。
12. 修改读写文件指令选择路径崩溃的问题。
13. 纠正汇川寸动及Jog运动与流程指令不一致的问题。
14. 设置点位接口函数增加获取汇川机器人姿态参数。
15. 常用变量与监控变量分离出来。
16. 平台增加Jump指令。
17. 弹框指令可设置弹框背景颜色。
18. 流程被修改保存后，关闭行拖动功能。
19. 增加打印点位修改的详细信息。
20. 欧姆龙PLC需要连接多个，SA1开放出来。
21. 增加检查工站中的轴使能是否断开，断开则工站不运动。

#### 2023.12.1
1. 变量模块的常用变量列表更改为绑定数据源方式。
2. 系统变量解除右键菜单删除及清除的禁用。
3. 解决汇川机器人走轨迹报错问题。
4. 解决工站走点比之前版本相对卡顿问题。
5. 优化结构体右键菜单，取消二级及三级菜单。
6. 优化串口接收数据功能。
7. 解决Epson机器人解析最后一个位置失败的问题。
8. 修改偏移量指令设置速度变量时保存不了的问题。
9. 点位修改函数增加限位检测，超过返回错误。
10. 修改流程指令变量名称更新不及时，更新的是上一次修改名称的问题。
11. 优化结构体插入数据项。
12. 增加"报警弹框背景颜色" ，"禁止实时保存结构体"两个系统变量。
13. 修改文件写入指令选择路劲出错问题。
14. 解决不同模块引用不同版本的Newtonsoft.Json造成的报警问题。
15. 优化位置锁存功能。
16. 变量模块增加系统变量操作防呆，默认系统变量不允许插入及删除操作。

#### 2024.1.1
1. 弹框指令不需要主动归入报警log，属性设置选项，可以根据需要自己选择打印。
2. 完善指令单位说明。
3. 修改步骤禁用后的显示无效问题。
4. 调整逻辑判断上下限显示错乱问题。
5. 修改流程指令速度设置大于100是生效的异常问题。
6. 汇川增加旋转优化方式设置。
7. 弹框指令不需要主动归入报警log。
8. 点位修改记录增加打印工站信息。
9. 完善指令时间单位说明。
10. 修改打开的第二个软件，关闭的时候有刹车被关闭的问题。
11. 修改步骤禁用后显示无禁用问题。
12. 解决逻辑判断上下限即变量常量显示错乱问题。
13. 解决IO调试关联2/3页面增加分割线，然后移动到最顶，会导致IO配置丢失问题。
14. 优化流程指令速度设置大于100是生效的问题。
15. 汇川机器人增加设置旋转优化。
16. 指令实现编译函数。
17. 流程实现继续功能。
18. 解决发送指令执行比较慢的问题。

#### 2024.2.1
1. 变量监控列表增加右键导出文件功能。
2. 解决工站点位出现多个“系统占用2”的问题。
3. 修改轴暂停时，流程停止不了问题。
4. 解决位置比较器使用位置锁存时停止比较器报错的问题。
5. 实现通讯指令根据名称更新通讯对象。
6. 弹框指令信息需要增加报警ID标识。
7. 结构体数据项可以按ctrl/shift选择多个,一起删除，一起转换类型。

#### 2024.3.1
1. 修改流程因状态问题，导致流程在暂停后卡死问题。
2. 结构体增加提示。
3. 磁盘满之后，增加文件保存防护。
4. 完善等待流程指令，等待流程必须要等待到流程正常结束，如果手动停止则继续等待。
5. 增加汇川总线卡模块

#### 2024.4.1
1. 增加清除登录信息的接口。
2. 对总线卡获取硬件状态进行加锁。
3. 设置字符型变量，如值为nll则设置无效。
4. 修改弹框加锁导致软件卡死问题。
5. 结构体增加重名检查。
6. 硬件信息模块开放机器人精度设置。
7. 修改暂停功能，不再是边缘触发，只要触发就会生效。
8. 位置比较增加详细log。
9. 主动弹框指令由之前的弹框后触发上报报警信息改为弹框前触发上报。
10. Jump指令增加点位到位检查，避免多余动作。
11. PLC增加欧姆龙UDP协议类型。
